# Divergence registry — TightCode vs `anomalyco/opencode`

Every file **modified** relative to upstream, with why + last merged upstream commit. Files not listed here must stay byte-identical to upstream (`01 brownfield-conventions.md`).

**Permanent rule (upstream merges):** every upstream workflow file that emerges at merge is deleted unless it runs on a standard runner (`ubuntu-latest`) **without** secrets. Upstream's battery (publish/deploy/containers/bots/blacksmith) validates the upstream project, not this fork; the fork's own `ci.yml` + gate cover its claims.

Last upstream merge: `bbd72fb8b` (dev, scaffold, 2026-09-05).

| File | Why it diverges | Last merged upstream commit |
|---|---|---|
| `AGENTS.md` | TightCode router section appended (upstream contributor conventions kept above) | `bbd72fb8b` |
| `README.md` | Fork README (upstream localized `README.*.md` translations removed — they presented OpenCode's branding/badges and routed users to upstream; a single-maintainer fork keeps English-only) | `bbd72fb8b` |
| `README.*.md` (21 files) | Deleted wholesale (upstream translations: OpenCode branding, links/badges to upstream). New upstream translations at merge are deleted. | `bbd72fb8b` |
| `SECURITY.md` | Fork reporting flow: points to `Iroh77/tightcode` Security Advisory + upstream-routing note (threat model kept — still factually true). | `bbd72fb8b` |
| `.gitignore` | `Second Cerveau` symlink line appended | `bbd72fb8b` |
| `.oxlintrc.json` | `ignorePatterns` += `packages/session-ui/src/v2/components/prompt-input/index.tsx`. The file itself stays byte-identical to upstream: its `content-['\200B']` (legacy octal escape) is a parse error for oxlint (tsgo/esbuild tolerate it, upstream ships it). The first fix attempt (`\200B` → `\u200B`) broke the rendered CSS — Tailwind emits the ZWSP only from the literal `\200B` candidate; `\u200B` produced a visible `u200B` in the composer placeholder. Temporary divergence — removed the day upstream fixes the escape. | `bbd72fb8b` |
| `.github/workflows/*` (26 files) | Deleted wholesale (see permanent rule above); `ci.yml` replaces them. Delete/modify conflicts at merge are resolved with `git rm` (30 s). | `bbd72fb8b` |
| `packages/opencode/src/effect/runtime-flags.ts` | R00-013: `disableLazyTools` flag (`OPENCODE_DISABLE_LAZY_TOOLS`) added (tool-lazy-loading kill-switch, SC-3). Ticket 09: `disableStaticSlimming` flag (`OPENCODE_DISABLE_STATIC_SLIMMING`) added (static-slimming kill-switch, SC-3). Ticket 13: `enablePromptCapture` flag (`OPENCODE_ENABLE_PROMPT_CAPTURE`) added (opt-in capture axis — default off IS the upstream-identical state, decision context-observability-01). Ticket 17 (R12-012): `disableToolWrapper` flag (`OPENCODE_DISABLE_TOOL_WRAPPER`) added (wrapper-axis kill-switch — flag set ⇒ binding sessions revert to schema-eager round-1 listing; `OPENCODE_DISABLE_LAZY_TOOLS` dominates). | `bbd72fb8b` |
| `packages/opencode/src/session/instruction.ts` | Ticket 09 (R11-001): root-wins gate on the `systemPaths` AGENTS.md walk — flag off ∧ `file === "AGENTS.md"` passes `findUp` output through `ContextSlimmer.rootWins`; global-files loop and config.instructions path untouched. | `bbd72fb8b` |
| `packages/opencode/test/session/instruction.test.ts` | Ticket 09: `withWorktree` git-worktree fixture + `Instruction.systemPaths root-wins (R11-001)` describe block appended; upstream describes untouched. | `bbd72fb8b` |
| `packages/opencode/test/session/system.test.ts` | Ticket 10 (R11-002): `RuntimeFlags` import + `SystemPrompt.mcpInstructions truncation (R11-002)` describe appended (seam tests, both renderers, flag passthrough). Ticket 11 (R11-003): `SystemPrompt.skills non-verbose (R11-003)` describe appended (non-verbose markdown, description-less filter, empty-list sentinel, flag ⇒ verbose XML); the upstream sorted-skills test's three `indexOf` markers updated `<name>X</name>` → `- **X**:` (intentional default-render change; sorting/stability property preserved). | `bbd72fb8b` |
| `packages/core/src/v1/config/config.ts` | Ticket 33 (R11-007): `ConfigV1.Info` gains the optional `system_prompt` record — built-in template name → inline text or `{ file }` — with a fork-marked schema annotation documenting the cascade and the inert-unknown-keys contract (malformed values are rejected loudly at config load, R00-010). | `bbd72fb8b` |
| `packages/opencode/src/session/llm.ts` | R12-008: `StreamInput.system` fork-extended `string[]` → `SystemBlock[]` (decision tool-lazy-loading-01 §4); live layer yields/wires `PromptBase` into `LLMRequestPrep.prepare`. Ticket 03: `StreamInput.toolSeeds` fork-extension (seeds channel into the freeze). Ticket 05: `StreamInput.toolVerdict` fork-extension (resolved R12-010 verdict travels to `reconcileTools`). Ticket 17 (R12-012): `StreamInput.toolWrapper` fork-extension (resolved wrapper axis travels beside the verdict). Ticket 13 (R10-001): live layer captures `Global.Service` at build (`Global.node` dep) and threads the app data dir into prepare as `data` — the capture sink is gated per turn in `request.ts`, so no per-turn Global context is needed. Ticket 33 (R11-007): `promptOverride: cfg.system_prompt` threaded into prepare (cfg is already resolved per run there). | `bbd72fb8b` |
| `packages/opencode/src/session/llm/request.ts` | R12-008/R12-007: `PrepareInput.system` → `SystemBlock[]` + `promptBase`; reconcile → render of frozen blocks before the upstream join; `small`/kill-switch bypass. Ticket 03: `reconcileTools` + impose (frozen listing projected at the payload, missing closures get a clearly-failing execute). Ticket 05 (R12-010): `PrepareInput.toolVerdict` rides into `reconcileTools`' mode; missing verdict defaults conservative binding (R12-010: missing sources ⇒ binding); local `ADVISORY` constant removed. Ticket 17 (R12-012): `PrepareInput.toolWrapper` rides into `reconcileTools`' wrapper (missing ⇒ false, round-1 semantics); impose's second loop tightened to pass through only non-frozen names — round-1 payload unchanged, wrapper sessions drop frozen deferred entries (the omission is the mechanism, decision tool-lazy-loading-04 §8). Ticket 13 (R10-001/002): `PrepareInput.data` (app data dir threaded from the LLM layer) + gated `PromptCapture.dump` as the last statement before `prepare`'s return — dump sees the exact returned object (SC-1), flag off = zero IO; `Prepared` otherwise untouched. Ticket 23 (R10-006): the dump's meta gains `toolServers` — tool name → sanitized MCP server assembled from the frozen listing entries (`server !== undefined` only), absent on bypass/small turns (no frozen base); payload untouched. Ticket 27 (R13-003/005): the dump's meta gains `verdict` (from `input.toolVerdict`, absent when undefined — no lazy branch ran: bypass/small/proxy/kill-switch turns, where `tools.resolve` returns no verdict under the flag) and `binary` (`InstallationVersion`, always); payload untouched.. Ticket 33 (R11-007): `PrepareInput.promptOverride` + `readOverrideFile` + the inline cascade at the position-0 seam — `SystemPrompt.base(model)` invoked once per turn, agent `prompt` (verbatim) > override > built-in; override values verbatim except the meta `{{MODEL_NAME}}` substitution; file refs exact-path against the instance working directory (`~/` expanded, no glob/ancestor walk), read fresh per turn, missing/unreadable fails the turn naming the resolved path (R00-010); unknown template keys inert; empty value = empty base; small turns apply the override (bypass skips PromptBase only). | `bbd72fb8b` |
| `packages/opencode/src/session/tools.ts` | Ticket 03 (R12-007): `SessionTools.resolve` builds the universe (source per producer, sanitized MCP server name) + `ToolListing.shape` seeds; per-turn AITool record rebuilt from the seeds' listing view; MCP loop wrapped in `!experimentalCodeMode` instead of early return. Ticket 05 (R12-010): resolve returns `{ tools, seeds, verdict }` — resolves `BindingVerdict` (with `Provider.Service`) in the lazy branch only, after the kill-switch early return (flag set ⇒ no resolution, `verdict: undefined`, upstream shapes); the resolved verdict renders the per-turn listing view. Ticket 07 (R12-006): deferred entries' execute wrapped once at the shaped-pass confluence via `ToolListing.withFallback` (covers built-in + resource + MCP closures uniformly; kill-switch branch returns earlier, so the fallback is inactive with the flag set). Ticket 17 (R12-012): resolve computes `wrapperActive = binding && !disableToolWrapper` and returns `wrapper` beside the verdict (kill-switch path: absent); on wrapper sessions the `deferred_tool` seed joins the universe before shape (unconditionally eager, decision tool-lazy-loading-04 §9); the listing render reads the `(verdict, wrapper)` pair; unlisted deferred entries keep full-fact AITool shapes (the payload omission happens at impose). Ticket 19 (R12-012): the withFallback wrap no longer depends on the listing view (wrapper sessions omit deferred views but their wrapped closures stay the dispatch targets, decision tool-lazy-loading-04 §4); when `wrapperActive` the meta-tool's AITool joins the shaped record last, closing over the completed record — `DeferredTool.dispatch` executor from `tool/deferred_tool.ts`. | `bbd72fb8b` |
| `packages/opencode/src/session/prompt.ts` | R12-008/SC-2: run loop assembles tagged `SystemBlock[]` (per-server `mcp:<server>` via `SystemPrompt.mcpBlocks`); `structured_output` stays a per-turn block. Ticket 05: `SessionTools.resolve` call site provides `BindingVerdict.Service`/`Provider.Service`, passes `toolVerdict` into `StreamInput`; layer deps gain `BindingVerdict.node`. Ticket 17 (R12-012): passes `toolWrapper` into `StreamInput` beside the verdict. Ticket 18 (R12-012): the block assembly appends `ToolListing.catalogBlocks` after skills when `resolved.wrapper` — deferred-tool discovery rides the frozen system region; advisory/kill-switch sessions emit no catalog keys (round-1 bytes unchanged). | `bbd72fb8b` |
| `packages/opencode/test/lib/llm-server.ts` | Ticket 05: probe-aware — the R12-010 binding probe is answered deterministically (text-only auto-reply, no tool call ⇒ conservative binding) without consuming the scripted queue or hits/calls accounting; upstream test assertions and choreography stay unmodified (the probe otherwise eats each test's first scripted reply). | `bbd72fb8b` |
| `packages/opencode/src/session/system.ts` | SC-2: `mcpBlocks` added (per-server sections, permissibility filter); upstream `mcp` refactored onto the shared `serverSection` helper — output bytes unchanged (system.test.ts green). Ticket 10 (R11-002): `mcpInstructions` truncation applied at the shared `visibleServers` seam (flag-gated `disableStaticSlimming`); layer gains `RuntimeFlags.Service`; `RuntimeFlags.node` added to the node deps; `serverSection` untouched. Ticket 11 (R11-003): `Skill.fmt` verbose option flipped to `{ verbose: flags.disableStaticSlimming }` (non-verbose markdown by default, verbose XML with the flag); stale verbose/lazy pairing comment dropped; skill-tool description untouched.. Ticket 33 (R11-007): `base(model)`/`render`/`TemplateName` extracted from `provider`'s body (same branches, same order, same outputs — behavior-preserving; the round-1 `system[0]` assertions stay green unchanged); `provider` is now the one-line composition `[render(base(model).raw, base(model))]`. | `bbd72fb8b` |
| `packages/opencode/test/provider/transform.test.ts` | `promptBase` passthrough stub added to a `LLMRequestPrep.prepare` call (new required input). Ticket 03: stub also implements `reconcileTools`. Ticket 06: stub gains the `entries` read-view member (new PromptBase.Interface method). Ticket 13: `data: "/tmp"` stub (prepare's app data dir input, capture sink off in the fixture). Ticket 17: stub's `reconcileTools`/`entries` gain the `wrapper` field (new PromptBase.Interface member). | `bbd72fb8b` |
| `packages/opencode/test/session/llm.test.ts` | `StreamInput.system` fixtures converted to `SystemBlock[]` (14 sites). | `bbd72fb8b` |
| `packages/opencode/test/session/llm-native-recorded.test.ts` | `StreamInput.system` fixture converted to `SystemBlock[]`. | `bbd72fb8b` |
| `packages/opencode/test/session/tools.test.ts` | Ticket 03: `SessionTools.resolve` call updated to the fork-extended `{ tools, seeds }` return. Ticket 05: layer factory (`baseLayer` flags/registry options) + lazy-listing describe (stubbed BindingVerdict: binding/advisory per-turn view, kill-switch never-resolves guard). Ticket 07: direct-call-fallback describe (failing deferred registry tool: schema append + delivery marker + `BindingVerdict.observe`; kill-switch leaves the failure upstream). Ticket 17 (R12-012): binding describe split into wrapper-on (seed eager + full-fact shapes kept, `wrapper: true`) and wrapper kill-switch (round-1 schema-eager bytes, no meta seed); advisory/kill-switch asserts extended with `wrapper` expectations. Ticket 19 (R12-012): `deferred_tool` meta-tool describe — construction gating (advisory/wrapper-off/kill-switch never construct), dispatch through the resolve confluence (happy path + unwrap metadata on the running part, unknown name, non-permissible name via a deny ruleset, schema-in-error + marker, inner `InvalidArgumentsError` riding the fallback with schema-once/marker-once/observe-fed). | `bbd72fb8b` |
| `packages/opencode/test/session/prompt.test.ts` | Ticket 17: `runtimeFlags` layer gains `disableToolWrapper: true` — these upstream-authored tests script direct calls to deferred tools (`glob`, `task`) and assert upstream behavior; the flag keeps their sessions upstream-identical (R00-013). No other change. | `bbd72fb8b` |
| `packages/opencode/src/tool/registry.ts` | Ticket 06 (R12-002/R00-013): `load_tool` registered in the builtin array behind the `disableLazyTools` gate (flag set ⇒ upstream registration set); `PromptBase.node` added to layer deps (LoadTool's read seam). | `bbd72fb8b` |
| `packages/opencode/src/acp/event.ts` | Ticket 20 (R12-012 presentation unwrap): the five part→ToolCall call sites (pending/running/duplicate-running/error/completed) pass `unwrapToolName(part)` as `toolName` — wrapper calls render with the inner tool's name/kind/title; pending unwraps via the wrapper's own `input.name` (ToolStatePending carries no metadata, the key is written at dispatch start). Parts themselves are published verbatim (read-only unwrap). The `part.tool === "bash"` snapshot check stays verbatim (deferred inner bash does not snapshot — accepted, design round-2 table). | `bbd72fb8b` |
| `packages/opencode/test/acp/event.test.ts` | Ticket 20: "attributes deferred_tool updates to the inner tool across the lifecycle" it appended (pending via input.name, running/error via the metadata key, completed title passthrough, part objects unmutated); upstream tests untouched. | `bbd72fb8b` |
| `packages/opencode/src/cli/cmd/stats.ts` | Ticket 20 (R12-012): per-tool usage counts `unwrapToolName(part)` — wrapper calls count under the inner tool's name; tokens/cost aggregation untouched. | `bbd72fb8b` |
| `packages/tui/src/routes/session/index.tsx` | Ticket 20 (R12-012 presentation unwrap, per-client): `toolprops.tool` and the Task subagent child-session `tools()` map unwrap via the local `deferredToolName` read (metadata key; pending keeps the wrapper name) — the correctly-labeled-generic option: renderer dispatch stays on `part.tool`, deferred_tool parts render GenericTool labeled with the inner tool's name. | `bbd72fb8b` |
| `packages/tui/src/util/tool-display.ts` | Ticket 20: `deferredToolName(tool, metadata)` added — the TUI-local unwrap read (cross-package: tui cannot import opencode internals; logic mirrors `unwrapToolName`'s metadata channel). | `bbd72fb8b` |
| `packages/tui/src/util/transcript.ts` | Ticket 20 (R12-012): `formatPart` exports a deferred_tool part's header under the inner tool's name (design round-2 TUI wire point); input/output export untouched (verbatim). | `bbd72fb8b` |
| `packages/tui/test/util/tool-display.test.ts` | Ticket 20: `deferredToolName` describe appended (metadata key, malformed fallbacks, non-wrapper passthrough); upstream describes untouched. | `bbd72fb8b` |
| `packages/tui/test/util/transcript.test.ts` | Ticket 20: "exports a deferred_tool part under the inner tool's name (R12-012)" test appended; upstream tests untouched. | `bbd72fb8b` |
| `packages/session-ui/src/components/message-part.tsx` | Ticket 20 (R12-012 presentation unwrap, per-client): `getToolInfo` switches on the unwrapped name — a deferred_tool part returns the inner tool's title/icon (title/icon only per the agreed seam; wrapper input not re-parsed); `ToolPartDisplay`'s Dynamic `tool` prop and `ToolErrorCard`'s `tool` prop unwrap; renderer dispatch stays on `part().tool` (GenericTool). | `bbd72fb8b` |
| `packages/opencode/test/tool/registry.test.ts` | Ticket 06: `withLazyDisabled` compile variant + load_tool expose/do-not-expose tests; upstream tests untouched. | `bbd72fb8b` |
| `packages/opencode/package.json` | Ticket 22 (R10-007/R00-012): two pinned devDependencies added, both dev-script-only (offline measurement cluster), never shipped runtime — no binary/desktop packaging impact. `js-tiktoken@1.0.21` (MIT license checked): the o200k reference encoding R10-007 names + the GLM adapter construction target (context-observability-03 §3 preferred variant). `@huggingface/tokenizers@0.2.0` (Apache-2.0 license checked): DeepSeek adapter — the design's named fallback variant, frozen by the walking skeleton (composed multi-regex pre-tokenizer cannot map to js-tiktoken's single `patStr`). | `bbd72fb8b` |
| `bun.lock` | Lockfile consequence of the ticket-22 devDependencies above (pinned, R00-012). | `bbd72fb8b` |

## TightCode-only additive files (do not exist upstream — safe at merge)

`opencode.json`, `CHANGELOG.md`, `docs/code-map.md`, `docs/divergence.md`, `docs/agents/*`, `scripts/verify.sh`, `.opencode/agent/researcher-code.md`, `.github/workflows/ci.yml`, `packages/opencode/src/session/llm/prompt-base.ts`, `packages/opencode/test/session/prompt-base.test.ts`, `packages/opencode/test/session/llm-request-prep.test.ts` (tickets 23, 27, 33), `packages/opencode/src/session/tool-listing.ts`, `packages/opencode/test/session/tool-listing.test.ts`, `packages/opencode/test/session/lazy-tools.integration.test.ts` (ticket 08), `packages/opencode/src/session/context-slimmer.ts` (feature 11), `packages/opencode/test/session/static-slimming.integration.test.ts` (feature 11), `packages/opencode/src/session/binding-verdict.ts` (tickets 04, 16), `packages/opencode/test/session/binding-verdict.test.ts` (tickets 04, 16), `packages/opencode/src/tool/load_tool.ts` + `load_tool.txt` (ticket 06), `packages/opencode/test/tool/load-tool.test.ts` (ticket 06), `packages/opencode/src/session/llm/prompt-capture.ts` (tickets 13, 27), `packages/opencode/test/session/prompt-capture.test.ts` (tickets 13, 27), `packages/opencode/test/session/prompt-capture.integration.test.ts` (ticket 13), `packages/opencode/script/measure-usage.ts` (ticket 14), `packages/opencode/test/usage-report.test.ts` (ticket 14), `packages/opencode/script/reference-workload.ts` + `packages/opencode/script/reference-workload/` (`prompts.ts`, `stub-bin.ts`, `fixture-repo/`, `README.md`; stub mirror extended by ticket 27) + `packages/opencode/test/reference-workload.test.ts` (tickets 15, 27), `packages/opencode/src/tool/deferred_tool.ts` + `deferred_tool.txt` (tickets 17, 19, 20, 21), `packages/opencode/test/tool/deferred-tool.test.ts` (tickets 19, 20), `packages/opencode/test/session/wrapper-session.integration.test.ts` (ticket 21), `packages/session-ui/src/components/deferred-tool-display.ts` + `deferred-tool-display.test.ts` (ticket 20), `packages/opencode/script/estimator.ts` + `packages/opencode/script/estimator/assets/` (`glm/tokenizer.json`, `deepseek/tokenizer.json`, `pins.json`) + `packages/opencode/test/estimator.test.ts` + `packages/opencode/test/fixture/estimator/` (ticket 22; parser `parseSystemBlocks`/`parseSkillsListing` added by ticket 24), `packages/opencode/script/breakdown.ts` + `packages/opencode/test/breakdown.test.ts` (tickets 24, 25; per-component token convention aligned to the R10-008 frozen payloadText encoding by ticket 26) + `packages/opencode/test/breakdown.integration.test.ts` (ticket 26), `packages/opencode/test/session/prompt-override.integration.test.ts` (ticket 34).