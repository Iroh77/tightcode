# Code map — TightCode

Committed map of the real code structure. Updated when the code changes, never left stale (`00 main.md`).

Layout: upstream `anomalyco/opencode` Bun workspace — the structure IS upstream's (`packages/*`). This map records where TightCode-specific behavior lives; see `docs/divergence.md` for the file-level registry.

Design source: `Second Cerveau/1 PROJETS/tightcode/ARCHITECTURE/basic-design.md` (V1 runtime target; payload-first principle).

| Zone | Location | Notes |
|---|---|---|
| Static slimming | `packages/opencode/src/session/context-slimmer.ts` (`ContextSlimmer.rootWins`, `ContextSlimmer.mcpInstructions`), `packages/opencode/src/session/instruction.ts`, `packages/opencode/src/session/system.ts` (`visibleServers` seam: flag-gated per-server instruction truncation feeding both `mcp` and `mcpBlocks`; `skills` seam: `Skill.fmt` verbose option flipped to `flags.disableStaticSlimming` — non-verbose markdown default, verbose XML with the flag; layer reads `RuntimeFlags.Service`, node deps include `RuntimeFlags.node`) | `ContextSlimmer` pure transforms edited in at producers: instructions root-wins (ticket 09, landed), mcp_instructions truncation (ticket 10, landed — word-boundary cut, `...` inside the ≤250 budget), skills non-verbose (ticket 11, landed — upstream's own `fmt` variant, no fork code); kill-switch `OPENCODE_DISABLE_STATIC_SLIMMING` (`effect/runtime-flags.ts`) |
| Tool listing policy | `packages/opencode/src/session/tools.ts` (`SessionTools.resolve`: universe + seeds + verdict + wrapper + shaped AITool record), `packages/opencode/src/session/tool-listing.ts` (`ToolListing.shape`/`render`/`catalogBlocks`/`schemaBlock`), `packages/opencode/src/permission/index.ts` | `ToolListing` (pure): permissible-only filter, eager/deferred seeds (full facts; `deferred_tool` joins EAGER — it only enters the universe on wrapper sessions; seeds carry `source` additively, frozen with the entry), listing view `render(entries, mode, wrapper)`: advisory ⇒ truncate100 + placeholder, binding ⇒ truncate100 + full schema, binding+wrapper ⇒ deferred entries omitted (R12-012) with prefix-once ownership computed over the full list before omission. `catalogBlocks({ seeds })` (R12-012 discovery, ticket 18): pure producer of the `<deferred_tools…>` block family — mcp ⇒ `catalog:<server>` (one block per server, `server="…"` attribute), resource ⇒ `catalog:resources`, else `catalog`; bullets `- name: truncate100(description)` (empty ⇒ name alone) in seed order; empty group emits nothing; mcp seed without server raises. `schemaBlock` (exported, ticket 19): the round-1 schema-in-error block shared by `withFallback` and `DeferredTool.dispatch`. Wiring: `resolve` builds the universe per producer (source, sanitized MCP server), resolves `BindingVerdict` in the lazy branch only (kill-switch ⇒ upstream shapes, empty seeds, no resolution, `wrapper` absent) and returns `{ tools, seeds, verdict, wrapper }`; `wrapperActive = binding && !disableToolWrapper` gates the unconditional eager `deferred_tool` seed push (definition facts from `tool/deferred_tool.ts`) and the meta-tool AITool construction |
| Prompt-base freeze | `packages/opencode/src/session/llm/prompt-base.ts` (`PromptBase`: `reconcileSystem`, `reconcileTools`, `entries` read view for load_tool, `render`), `packages/opencode/src/session/llm/request.ts` (`LLMRequestPrep.prepare` wiring: reconcile + impose projection at the payload + `small`/flag bypass), `packages/opencode/src/session/llm.ts` (`StreamInput.system`/`toolSeeds`/`toolVerdict`/`toolWrapper` fork-extension), `packages/opencode/src/session/prompt.ts` (tagged-block producer + catalog emission when `resolved.wrapper`, seeds producer), `packages/opencode/src/session/system.ts` (`mcpBlocks` per-server sections) | R12-008/R12-007: first-write-wins per `(sessionID, provider/model/endpoint)` for system blocks AND tool entries, batched append-only reveal, frozen `(mode, wrapper)` policy pair, `structured_output` per-turn passthrough. `SystemBlockKey` catalog family (`catalog`, `catalog:resources`, `catalog:<server>` — R12-012, ticket 18): frozen/append-only like every key; `render` projects the catalog slot after skills and before per-turn keys in frozen append order (no wrapper tags — blocks carry their own delimiters); sessions without catalog keys render round-1 bytes exactly. Impose projects frozen entries into the payload (names the per-turn record no longer produces stay listed with a clearly-failing execute) and passes through only non-frozen names — on wrapper sessions frozen deferred entries are dropped from the payload (R12-012, the omission is the mechanism); kill-switch `OPENCODE_DISABLE_LAZY_TOOLS` (`packages/opencode/src/effect/runtime-flags.ts`) |
| Binding verdict | `packages/opencode/src/session/binding-verdict.ts` (`BindingVerdict.resolve`/`observe`, `layerWith({ staticTable, probe })` seam) + XDG app data cache `binding-verdicts.json`; resolved at `SessionTools.resolve` (strictly before the session's first verdict-consuming provider request) and carried per turn as `StreamInput.toolVerdict` into the frozen base | R12-010: static table → cross-session JSON cache (SC-4, tolerant IO) → adversarial probe (record_answer, 10s timeout) → binding on unresolvable; `observe` learns binding→advisory (sticky) — Amendment 4 (ticket 37): gated upstream, fed only on wrapper-active binding sessions (schema-eager/advisory-resolved sessions pass none — no self-sealing); each flip logs key/tool/args digest at the verdict layer and persists `violation` evidence `{kind:"violation",tool,args}` (args JSON capped 2048) in the cache entry + memo provenance, additive beside probe evidence (legacy entries valid); memoized per (provider, model, endpoint) per instance; `classifyProbeResponse` pure parse seam — Amendment 3 (ticket 35, decision tool-lazy-loading-05 §1): the decoy registers `PROBE_DECOY_SCHEMA` = the R12-003 advisory placeholder (`{type:"object",properties:{}}`, equality-tested against `ToolListing.PLACEHOLDER`), the adversarial constraint lives in the prompt, and the classify boundary is any emitted argument ⇒ advisory, `{}`/absent ⇒ binding, non-record ⇒ unparseable failure ⇒ binding. R12-013 provenance (ticket 36): `resolve` returns `ResolvedVerdict = { verdict, provenance }` — `VerdictProvenance` discriminates pin / static-table / cache (`source` + `timestamp`, + `evidence?` when the entry carries it) / probe (`evidence`) / default (nothing learnable ⇒ binding without evidence); the memo stores the full pair (a hit reports the populating origin, never "memo"); the probe returns `ProbeOutcome = { verdict, evidence }` (`probeOutcomeFromCall` pure mapper: call args verbatim JSON capped 2048 / no-tool-call / failure class timeout·transport·unparseable, transport = defaultProbe's own request-failure catch, timeout = cascade's `timeoutOption` branch, both logged) and persists its evidence additively in the cache entry (`evidence?`, legacy entries valid); cascade-exit `logInfo` emits `{key, verdict, provenance}` per branch |
| Probe disclosure + verdict pin | `packages/opencode/src/session/binding-verdict.ts` (`PROBE_PROMPT` + `recordAnswer` disclosure text, exported for the exact-string tests; pin input `layerWith({ pin })` or env `OPENCODE_PIN_BINDING_VERDICT`, cascade step 0 ahead of the memoized state/static table/cache/probe) | R12-011 text-only — disclosure strings verbatim (re-pinned by round 3, ticket 35: pass-the-answer-as-argument constraint, empty decoy); R13-003 harness-only pin: invalid/empty value logged + ignored, `observe` still writes, pin wins every resolve |
| load_tool + fallback | `packages/opencode/src/tool/load_tool.ts` + `load_tool.txt` (bulk load, serve off the frozen `(mode, wrapper)` pair, `delivered()` predicate over the filtered history, delivery marker via `ExecuteResult.metadata`), `packages/opencode/src/session/tool-listing.ts` (`ToolListing.withFallback`: failed deferred call appends the full schema to the error text, marks the running part loaded, excludes permission denials + aborts, feeds `InvalidArgumentsError` to the caller-wired `observe` when present (optional args-carrying factory, Amendment 4 gate); the delivery-marker write waits for the running part with a bounded retry (the pending→running state rebuild drops earlier writes — the race deferred_tool.ts documents, unmasked by the ticket-37 gate); `schemaBlock` — the shared round-1 schema-block format), `packages/opencode/src/session/tools.ts` (deferred executes wrapped once at the shaped-pass confluence — built-in + resource + MCP uniformly, wrap independent of the listing view so wrapper-session deferred closures stay withFallback-wrapped for dispatch; kill-switch branch returns before the wrap), `packages/opencode/src/tool/registry.ts` (registration behind the `disableLazyTools` gate, `PromptBase.node` dep) | R12-004/005 (ticket 06): first load = full description + schema (advisory) / description only (binding), confirmation on delivered loads, re-serve after compaction (`time.compacted` respected), per-name error lines never fatal; R12-006 (ticket 07): direct call executes, failure of a not-yet-delivered tool recovers protocol-level via the schema block in the error output; R12-012 (ticket 17): binding+wrapper serves description + schema again (no listing entry carries the schema); missing frozen wrapper defaults false |
| deferred_tool meta-tool | `packages/opencode/src/tool/deferred_tool.ts` + `deferred_tool.txt` (closed `{name, args}` schema, description, `dispatch` executor), `packages/opencode/src/session/tools.ts` (confluence: meta-tool AITool constructed only when `wrapperActive`, closing over the completed shaped record), `test/tool/deferred-tool.test.ts` + `test/session/tools.test.ts` (dispatch seam + confluence tests) | R12-012 (ticket 19): dispatch validates `name` against the per-turn shaped deferred seeds only (permissible — unknown/denied ⇒ pinned "Unknown deferred tool" error, eager ⇒ "call it directly"; no schema, no marker), writes `metadata.deferred_tool = { tool }` on the running part at dispatch start (failToolCall preserves it), on unparseable/non-object `args` delivers the inner full schema block + `load_tool` marker (schema-in-error, the binding-session stand-in for R12-006), routes through the withFallback-wrapped inner closure (permission/plugin/fallback unwrap naturally — no wrapper-level triggers, no double-append), and merges `deferred_tool` into the success output's metadata (survives completeToolCall's overwrite) |
| Deferred-tool wrapper (payload core) | `packages/opencode/src/session/tool-listing.ts` (`render(entries, mode, wrapper)` — deferred entries omitted under binding+wrapper, EAGER gains `deferred_tool`), `packages/opencode/src/session/tools.ts` (`wrapperActive = binding && !disableToolWrapper` + unconditional eager seed push + meta-tool confluence), `packages/opencode/src/session/llm/prompt-base.ts` (wrapper frozen at first write next to mode), `packages/opencode/src/session/llm/request.ts` (impose drops frozen deferred entries on wrapper sessions), `packages/opencode/src/effect/runtime-flags.ts` (`disableToolWrapper` → `OPENCODE_DISABLE_TOOL_WRAPPER`), `packages/opencode/test/session/wrapper-session.integration.test.ts` (ticket 21 end-to-end: eager-only payload + catalog, dispatch + unwrap metadata + verbatim history, schema-in-error + marker + confirmation, wrapper-mode `load_tool` serve, byte stability, kill-switch, advisory regression guard; binding forced via the verdict pin seam). Ticket 21 refinement: the dispatch metadata writes wait for the running part (`writeMetadataWhenRunning`, bounded poll) because the ai-sdk can start execute before the processor consumes the stream's `tool-call` part and the pending→running rebuild drops earlier writes | R12-012 (tickets 17, 18, 19, 21): binding+wrapper drops deferred listing entries at the payload; closed schema `{name, args}`; three-way load_tool serve; catalog blocks = keyed system-block family; end-to-end verified at the payload and history channels |
| Presentation unwrap | `packages/opencode/src/tool/deferred_tool.ts` (`unwrapToolName(part)` — metadata key `deferred_tool.tool` first, the wrapper's own `input.name` as the pending fallback under the deferred_tool gate; pure, read-only), `packages/opencode/src/acp/event.ts` (five part→ToolCall call sites pass the unwrapped name as `toolName` — inner name/kind/title across pending/running/error/completed; `bash` snapshot check stays verbatim), `packages/opencode/src/cli/cmd/stats.ts` (per-tool usage counts the inner name), `packages/tui/src/util/tool-display.ts` (`deferredToolName` — TUI-local read, cross-package) + `packages/tui/src/routes/session/index.tsx` (`toolprops.tool` + Task subagent child-session `tools()` unwrap; renderer stays GenericTool — correctly-labeled generic) + `packages/tui/src/util/transcript.ts` (export header unwrapped), `packages/session-ui/src/components/deferred-tool-display.ts` (`deferredToolName` — session-ui-local read) + `message-part.tsx` (`getToolInfo` switches on the unwrapped name — inner title/icon; Dynamic `tool` prop + `ToolErrorCard` `tool` prop unwrapped; renderer dispatch stays on `part().tool`) | R12-012 (ticket 20): presentation-layer only — stored parts, SSE events and the model payload keep `tool: "deferred_tool"` (ACP lifecycle test asserts the published parts unmutated; payload asserted end-to-end by ticket 21). Pending carries no metadata by schema, so ACP pending unwraps via `input.name` (human-approved seam); TUI/session-ui pending keeps the wrapper name until the running update. Secondary session-ui readers (grouping/visibility) deferred — default hidden/generic behavior accepted (design round-2 table) |
| Prompt capture | `packages/opencode/src/session/llm/prompt-capture.ts` (module) + `packages/opencode/src/session/llm/request.ts` (gated sink at `prepare`'s return; `PrepareInput.data` = app data dir threaded from `packages/opencode/src/session/llm.ts`, where `Global.Service` is a build-time dep) + `packages/opencode/src/effect/runtime-flags.ts` (`enablePromptCapture`) + app data dumps | PromptCapture: `{meta, payload:{system, tools, messages}}` per turn under `data/prompt-captures/<sessionID>/`, whitelist-serialized (R10-002: params/headers/auth never enter), stateless seq from dir scan, opt-in `OPENCODE_ENABLE_PROMPT_CAPTURE` (SC-3 capture row), dump failure logged never fatal; ticket 13. `meta.toolServers?: Record<string, string>` (ticket 23, R10-006): tool name → sanitized MCP server for the breakdown's MCP view, assembled at the dump call site from the frozen listing entries (`server !== undefined` only), absent on bypass/small turns (no frozen base) — additive optional, `version` stays 1, tolerant readers (SC-4). `meta.verdict?: "binding" | "advisory"` + `meta.binary?: string` (ticket 27, R13-003/005, SC-4): the lazy branch's resolved verdict from `StreamInput.toolVerdict` (absent when none ran — bypass/small/proxy/kill-switch turns, where `tools.resolve` returns no verdict under the flag) and the producing binary's `InstallationVersion`, assembled at the same dump call site; additive optional, harness v2 consumers (feature 13) read them tolerant-read. `meta.verdictProvenance?: VerdictProvenance` (ticket 36, R12-013): how the verdict was reached, from `StreamInput.toolVerdictProvenance` next to `toolVerdict` at the same dump call site — same omission rule (absent when no lazy branch ran) |
| Kill-switches | `packages/opencode/src/effect/runtime-flags.ts` (pattern), per-axis `OPENCODE_*` flags (`OPENCODE_DISABLE_LAZY_TOOLS`, `OPENCODE_DISABLE_STATIC_SLIMMING`, `OPENCODE_DISABLE_TOOL_WRAPPER` (R12-012 wrapper axis — binding reverts to schema-eager round-1; lazy-tools flag dominates), `OPENCODE_ENABLE_PROMPT_CAPTURE` opt-in) | R00-013: flag set = upstream-identical per axis |
| Measurement | `packages/opencode/script/measure-usage.ts` + `test/usage-report.test.ts` (ticket 14); `packages/opencode/script/reference-workload.ts` + `script/reference-workload/` (`prompts.ts` fixed list, `fixture-repo/` deterministic tree, `stub-bin.ts` echo stub, `README.md` baseline procedure) + `test/reference-workload.test.ts` (ticket 15) | measure-usage: pure `report`/`diff` cores + run-dir readers (`manifest.json` schema-validated via exported `RunManifestSchema`, DB `step-finish` parts authoritative via `bun:sqlite` read-only, captures tolerant-read) + CLI (`report <runDir> [-o out.json]` / `diff <fork> <upstream> [-o out.json]`, loud offline errors); pairing captures↔step-finish by order, chars/4 estimator reconciled to `usage.input` (residual → history), cache columns raw; diff aligns by index with `promptsDigest` guard + mismatch warnings. reference-workload driver: `--bin/--run-dir/--model/--proxy/--stub` → one isolated run dir per run (`cwd/` fixture copy + `opencode.json` model pin/temp 0, `data/` = `XDG_DATA_HOME` + `OPENCODE_DB`, seeded `auth.json`, capture env always set — upstream ignores it, fork captures), `--proxy` adds both optimization kill-switches, prompts chained on one session via `--continue`, manifest written at completion with the workload digest (prompts + fixture content); stub bin writes deterministic step-finish rows + capture dumps + `stub-env.json` (observed env flags, incl. `OPENCODE_PIN_BINDING_VERDICT`; captures mirror the harness v2 meta — `binary: "stub"`, `verdict` mapped from the pin env `binding`/`advisory` → same, anything else absent, ticket 27; pinned captures also carry `verdictProvenance: {origin:"pin"}` (ticket 36, R12-013); baseline procedure for the R10-005 closeout in `script/reference-workload/README.md`. Harness v2 seams (ticket 28, R13-001/003/005): `RunManifestSchema` gains additive optional `pin`/`verdicts`/`binary` (v1 manifests read as null/[]); pure metric exports `coldStartInput` (first usage row = first provider turn), `sessionInput` (Σ input+cacheRead+cacheWrite), `payloadChars` (v1 char accounting without ceil — shared `payloadCharComponents` with the estimator), `cacheCollapseFlags` + `CACHE_COLLAPSE_RATIO = 0.5` (diagnostic-only), `deriveVerdicts` (distinct sorted meta.verdict), `deriveVerdictProvenances` (R12-013, ticket 36: distinct sorted meta.verdictProvenance JSON values — manifest-only, never consumed by the gate; `RunManifestSchema` gains additive optional `verdictProvenances`, written by the driver next to `verdicts`, old runs valid), exported `readCaptures`; driver gains `--pin <binding|advisory>` (child env `OPENCODE_PIN_BINDING_VERDICT`, invalid value fails loudly) and post-run v2 manifest derivation: `verdicts` from the run's captures, `binary` = first non-empty `<bin> --version` stdout line (`"stub"` in stub mode; failure → null + warning, manifest still written). Campaign runner (ticket 29, R13-002/003, comparison-testing-01 §4): `--campaign <spec.json> --out <dir> [--stub]` — exported `validateSpec` (loud: duplicate phase names, zero phases/legs, `runs >= 2` never single runs, filesystem-safe phase names since they embed in run dirs, `<provider/model>` shape, 32-bit seed range, `pin` literals; fills the default N=3) + exported `scheduleRuns` (per phase, legs×N Fisher–Yates shuffled with a classic 32-bit LCG seeded from `spec.seed`, default `crypto.getRandomValues`, recorded; strictly sequential; run dir `<out>/<index:00>-<phase>-<shape>[-proxy]`); shared `executeRun` per-run path (single-run mode unchanged, campaign passes phase model + `mcp` fragment merged into the generated `opencode.json`, leg bin/proxy, `capture` env on fork-shape legs only — upstream-leg stub dirs get no captures, pin env every leg when `spec.pin`); progressive `<out>/campaign.json` (exported `CampaignSchedule`: spec echo + seed + startedAt/endedAt + per-run `{index, phase, shape, proxy, rep, runDir, status, error?}`) written up front all-pending and after every run; first failure aborts (`failed` + error + `endedAt`, exit non-zero, later runs untouched pending); non-empty out dir refused; single-run flags rejected in campaign mode |
| Token estimator | `packages/opencode/script/estimator.ts` + `test/estimator.test.ts` (ticket 22) + `script/estimator/assets/` (`glm/tokenizer.json` 20MB GLM-5.3-Flash, `deepseek/tokenizer.json` DeepSeek-V4-Flash-0731, `pins.json` sha256+source+revision+bytes) + `test/fixture/estimator/` (cold-start capture + real step-finish usage row per pinned model, mined from the 2026-09-13 baseline runs) | R10-007/008 (SC-5): `forModel(modelID, {assetsDir?})` → `{modelID, adapter, estimate}` — prefix select on the lowercased last path segment (`glm*`/`deepseek*`, else o200k), sync factory, lazy load + sha256-pin verify at first estimate, memoized per process; GLM = pure-JS conversion (HF BPE vocab→js-tiktoken rank table via byte-unicode inversion, asset's own pre-tokenizer regex; proven 49/49 exact vs HF python oracle); DeepSeek = `@huggingface/tokenizers` native (composed multi-regex pre-tokenizer not expressible as js-tiktoken `patStr` — the design's named fallback, frozen by the slice); unavailable/corrupt/mismatched asset → logged `o200k-fallback`, never thrown; special tokens never added (DeepSeek: `added_tokens` filtered to non-special; GLM: added tokens out of the rank table — non-special added tokens count as BPE pieces, documented limitation); `verifyAssets` + CLI `verify` subcommand loud non-zero; R10-008 bound = Σ(input+cacheRead+cacheWrite) within ±10% with adapter-engagement assertion (deviations human-approved 2026-09-15: exact-model assets, Σ bound reference). Payload-encoding convention frozen by the bound test: system joined, tools as `{type:"function",function:{name,description,parameters}}` JSON, messages JSON after dropping leading role:"system". Ticket 24 adds the SC-5 rendered-format parsers (`parseSystemBlocks` line-based anchor scanner + tiling, `parseSkillsListing` both `Skill.fmt` modes) — sole owner of rendered-format parsing per R10-006 |
| Comparison harness v2 | `packages/opencode/script/measure-usage.ts` (cache-immune metric functions, campaign aggregation, v2 report), `packages/opencode/script/reference-workload.ts` (campaign runner, verdict pin flag), `packages/opencode/script/reference-workload/README.md` (campaign procedure), manifest schema extension (binary identity, verdict, pin) | R13-001..006; real-model legs stay offline (CI unchanged). Landed: manifest v2 fields, `--pin`, binary identity, metric functions + seam tests (ticket 28); campaign runner + seeded schedule + campaign.json (ticket 29); campaign aggregation + ComparisonReport v1 + CLI (ticket 30): `campaign <dir> [-o out.json]` over pure `campaignReport` — campaign.json tolerant-additive read (loud on missing/invalid), incomplete-campaign refusal (any non-`done` run or null `endedAt`), legs grouped by (phase, shape, proxy), per-leg medians + dispersion `{median, min, max, values}` for `coldStartInput`/`sessionInput`/`payloadChars` (null where unavailable, partial availability warned), verdict column + `verdictMixed` via shared `verdictBreakdown`, per-run verdicts/binary listing, cache-collapse diagnostics with runDir; per-phase `fork-vs-upstream` pair + `fork-vs-fork-proxy` pair adding `payloadChars` (deltas = median(fork) − median(other)); gate = fork-side uniformity and recorded (upstream/proxy exempt; flip/mixed/unrecordable → `comparable: false` + warning naming run dirs, medians still emitted); estimator-sourced cold-start attribution (ticket 31): exported `runAttribution(run)` runs feature 10's `breakdown()` core per run (turn 0 of the first session's `system`/`tools`/`history` totals — one implementation of attribution, dynamic import so the breakdown→measure-usage readers cycle stays broken; null on capture-less runs) and `legAttribution` medians them per leg into `LegReport.coldStartAttribution` (same availability rule as the other columns via shared `availableValues`; absent from pair metrics — SC-5's per-leg figure for the fork-vs-fork-proxy reading); seam tests in `test/usage-report.test.ts` "campaign aggregation (v2 report)" over synthetic campaign dirs, drift test runs both entry points (breakdown() directly vs campaignReport) on one fixture. Integration + README (ticket 32, R13-004/006): `test/campaign.integration.test.ts` runs the stub campaign end to end (driver + measure-usage CLI spawned; scenarios: seeded 8-run schedule + v2 manifests, per-leg capture asymmetry, schema-1 report with degenerate dispersions + verdict columns + comparable pairs, unpinned → comparable-false + warning, runs:1 + interrupted-campaign loud failures) plus the R13-004 structural assert on `prompts.ts`; `README.md` gains the campaign-mode section (spec example with an mcp phase, seeded reproducibility, pin guidance, verdict/flip semantics, medians interpretation, binary identity, CI-offline note) and the R13-004 prompt-authority note — prompts are owner-authored committed literals, edits change the digest and invalidate recorded runs, mutating prompts stay self-contained |

| Config prompt override | `packages/core/src/v1/config/config.ts` (`ConfigV1.Info.system_prompt` optional record: built-in template name → inline text or `{ file }`; fork-marked annotation, malformed values rejected at load), `packages/opencode/src/session/system.ts` (`base(model)` → `{template, raw, name}` + `render(text, {name})` + `TemplateName` — extracted from `provider`'s body, same branches/order/outputs; `provider` = one-line composition, output byte-identical), `packages/opencode/src/session/llm/request.ts` (`readOverrideFile` + the inline cascade at the position-0 seam), `packages/opencode/src/session/llm.ts` (`promptOverride: cfg.system_prompt` threading), `packages/opencode/test/session/llm-request-prep.test.ts` (`prompt-override` describe, ticket 33), `packages/opencode/test/session/prompt-override.integration.test.ts` (`override cascade end-to-end`, ticket 34 — six scenarios on the fixture provider: inline override + downstream round-1 invariants, file-ref against the instance dir, missing-file loud failure with zero provider requests, config-agent cascade top, inert key, R11-005 `base()`/`provider` re-assertion) | R11-007 as amended (ticket 33; no kill-switch — capture-precedent class, decision `context-assembly-02`): cascade agent `prompt` (verbatim) > override > built-in at `SystemPrompt.base(model)`, invoked once per turn; override values verbatim except the meta `{{MODEL_NAME}}` substitution (render is the built-in's own step, shared); file refs exact-path against the instance working directory (`~/` expanded via `os.homedir()`, no glob/ancestor walk), read fresh per turn, missing/unreadable fails the turn naming the resolved path (R00-010); unknown template keys inert by contract; empty value = empty base (explicit no-base slimming; upstream's `.filter` drops the empty string, so system[0] renders blocks-only); small turns apply the override (bypass skips PromptBase only); the base text never enters the keyed-block freeze — config/file changes apply next turn. Built-in defaults byte-identical (R11-005). Integration test landed (ticket 34, 2026-09-16) |

## Planned — round 2 basic design (names only; nothing implemented yet)

Source: `Second Cerveau/1 PROJETS/tightcode/ARCHITECTURE/basic-design.md` round-2 section + `decisions/basic-design-03.md`. Move rows into the table above as they land.

| Zone | Planned locations | Notes |
|---|---|---|
| Breakdown command | `packages/opencode/script/breakdown.ts` + `packages/opencode/test/breakdown.test.ts`; parser in `packages/opencode/script/estimator.ts` (`parseSystemBlocks`, `parseSkillsListing`, pinned by `test/estimator.test.ts` fixture strings + the committed glm real-render capture) | R10-006/009: `breakdown({manifest, captures, usage})` → Breakdown JSON v1 (`schema: 1`; per-turn system block rows keyed `base`/`environment`/`instructions`(folded)/`mcp:<server>`/`skills`/`catalog[:<server>]`/`structured_output`/`user_system`/`other`, `mcpInstructionsTags`, skills sub-view via `parseSkillsListing`, per-tool rows with `server` attribution, per-message history rows post-leading-system, MCP cross-cut view = grouping dimension never in category totals, unreconciled `inputEstimate`, output/inputReported pairing, session+run rollups with null-propagating totals); attribution = `meta.toolServers` map, else scoped longest-`server_` prefix fallback against capture-visible servers, else `unattributed`; wrapper-session deferred MCP tools enter the per-server view as catalog bullets (record entries take precedence); sessions = usage order (DB rowid) + capture-only sessions, order-zip pairing, unpaired usage warned + empty slot; parser tiling invariant (segments partition the joined system bytes; `</env>` closes env, references header re-anchors environment, structured_output single-line, trailing gap = `user_system`); `--content` drill-down (ticket 25, R10-009): additive options param `breakdown(input, { content })` — selection lives in the core because the projected JSON carries no raw texts; keys `system.<block>`, `system.skills:<name>`, `tools.<name>`, `history.<index>`, selected nodes gain `content` (blocks = raw parser segment text, byte-exact incl. separators, so contents reconstruct the system payload; tools = `{description, inputSchema}`; history text-or-JSON; skills item text), a key valid in any turn is accepted, unknown/malformed keys throw listing the available vocabulary; CLI (ticket 25): exported `main(argv)` + `import.meta.main` guard — `<runDir> [--session <id>] [--turn <n>] [--content <key>...] [--human] [-o out.json]` over `loadRun`, catch-inside-main → non-zero + message (R00-010), `-o` = measure-usage's emit pattern, `--human` stdout projection (counts + % of `inputEstimate`, never a second format of record; mutually exclusive with `-o` and `--content`), `--session`/`--turn` reader-side filters with totals recomputed over what remains (`--turn` drops sessions left without that index); default output counts-only; per-component token convention (ticket 26): tools = the wire's `{"type":"function","function":{name,description,parameters}}` JSON, history messages = full message JSON, system = joined payload — byte-aligned with the R10-008 bound test's frozen payloadText encoding, so `inputEstimate` = the bound convention partitioned by category (the bare name+schema+description assembly undercounted deepseek past ±10 % through the full path) + `test/breakdown.integration.test.ts` (ticket 26): fixture run dir built in-test (manifest + bun:sqlite DB + captures over the real `Skill.fmt`/`ToolListing.catalogBlocks` producers) → `loadRun` → `breakdown()` → v1 JSON → CLI emit; scenarios 1-7 incl. R10-008 bound fixtures re-asserted through the full path with adapter engagement |
