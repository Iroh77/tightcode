# Code map — TightCode

Committed map of the real code structure. Updated when the code changes, never left stale (`00 main.md`).

Layout: upstream `anomalyco/opencode` Bun workspace — the structure IS upstream's (`packages/*`). This map records where TightCode-specific behavior lives; see `docs/divergence.md` for the file-level registry.

Design source: `Second Cerveau/1 PROJETS/tightcode/ARCHITECTURE/basic-design.md` (V1 runtime target; payload-first principle).

| Zone | Location | Notes |
|---|---|---|
| Static slimming | `packages/opencode/src/session/context-slimmer.ts` (`ContextSlimmer.rootWins`, `ContextSlimmer.mcpInstructions`), `packages/opencode/src/session/instruction.ts`, `packages/opencode/src/session/system.ts` (`visibleServers` seam: flag-gated per-server instruction truncation feeding both `mcp` and `mcpBlocks`; `skills` seam: `Skill.fmt` verbose option flipped to `flags.disableStaticSlimming` — non-verbose markdown default, verbose XML with the flag; layer reads `RuntimeFlags.Service`, node deps include `RuntimeFlags.node`) | `ContextSlimmer` pure transforms edited in at producers: instructions root-wins (ticket 09, landed), mcp_instructions truncation (ticket 10, landed — word-boundary cut, `...` inside the ≤250 budget), skills non-verbose (ticket 11, landed — upstream's own `fmt` variant, no fork code); kill-switch `OPENCODE_DISABLE_STATIC_SLIMMING` (`effect/runtime-flags.ts`) |
| Tool listing policy | `packages/opencode/src/session/tools.ts` (`SessionTools.resolve`: universe + seeds + verdict + wrapper + shaped AITool record), `packages/opencode/src/session/tool-listing.ts` (`ToolListing.shape`/`render`/`catalogBlocks`/`schemaBlock`), `packages/opencode/src/permission/index.ts` | `ToolListing` (pure): permissible-only filter, eager/deferred seeds (full facts; `deferred_tool` joins EAGER — it only enters the universe on wrapper sessions; seeds carry `source` additively, frozen with the entry), listing view `render(entries, mode, wrapper)`: advisory ⇒ truncate100 + placeholder, binding ⇒ truncate100 + full schema, binding+wrapper ⇒ deferred entries omitted (R12-012) with prefix-once ownership computed over the full list before omission. `catalogBlocks({ seeds })` (R12-012 discovery, ticket 18): pure producer of the `<deferred_tools…>` block family — mcp ⇒ `catalog:<server>` (one block per server, `server="…"` attribute), resource ⇒ `catalog:resources`, else `catalog`; bullets `- name: truncate100(description)` (empty ⇒ name alone) in seed order; empty group emits nothing; mcp seed without server raises. `schemaBlock` (exported, ticket 19): the round-1 schema-in-error block shared by `withFallback` and `DeferredTool.dispatch`. Wiring: `resolve` builds the universe per producer (source, sanitized MCP server), resolves `BindingVerdict` in the lazy branch only (kill-switch ⇒ upstream shapes, empty seeds, no resolution, `wrapper` absent) and returns `{ tools, seeds, verdict, wrapper }`; `wrapperActive = binding && !disableToolWrapper` gates the unconditional eager `deferred_tool` seed push (definition facts from `tool/deferred_tool.ts`) and the meta-tool AITool construction |
| Prompt-base freeze | `packages/opencode/src/session/llm/prompt-base.ts` (`PromptBase`: `reconcileSystem`, `reconcileTools`, `entries` read view for load_tool, `render`), `packages/opencode/src/session/llm/request.ts` (`LLMRequestPrep.prepare` wiring: reconcile + impose projection at the payload + `small`/flag bypass), `packages/opencode/src/session/llm.ts` (`StreamInput.system`/`toolSeeds`/`toolVerdict`/`toolWrapper` fork-extension), `packages/opencode/src/session/prompt.ts` (tagged-block producer + catalog emission when `resolved.wrapper`, seeds producer), `packages/opencode/src/session/system.ts` (`mcpBlocks` per-server sections) | R12-008/R12-007: first-write-wins per `(sessionID, provider/model/endpoint)` for system blocks AND tool entries, batched append-only reveal, frozen `(mode, wrapper)` policy pair, `structured_output` per-turn passthrough. `SystemBlockKey` catalog family (`catalog`, `catalog:resources`, `catalog:<server>` — R12-012, ticket 18): frozen/append-only like every key; `render` projects the catalog slot after skills and before per-turn keys in frozen append order (no wrapper tags — blocks carry their own delimiters); sessions without catalog keys render round-1 bytes exactly. Impose projects frozen entries into the payload (names the per-turn record no longer produces stay listed with a clearly-failing execute) and passes through only non-frozen names — on wrapper sessions frozen deferred entries are dropped from the payload (R12-012, the omission is the mechanism); kill-switch `OPENCODE_DISABLE_LAZY_TOOLS` (`packages/opencode/src/effect/runtime-flags.ts`) |
| Binding verdict | `packages/opencode/src/session/binding-verdict.ts` (`BindingVerdict.resolve`/`observe`, `layerWith({ staticTable, probe })` seam) + XDG app data cache `binding-verdicts.json`; resolved at `SessionTools.resolve` (strictly before the session's first verdict-consuming provider request) and carried per turn as `StreamInput.toolVerdict` into the frozen base | R12-010: static table → cross-session JSON cache (SC-4, tolerant IO) → adversarial probe (record_answer, 10s timeout) → binding on unresolvable; `observe` learns binding→advisory (sticky); memoized per (provider, model, endpoint) per instance; `classifyProbeResponse` pure parse seam |
| Probe disclosure + verdict pin | `packages/opencode/src/session/binding-verdict.ts` (`PROBE_PROMPT` + `recordAnswer` disclosure text, exported for the exact-string tests; pin input `layerWith({ pin })` or env `OPENCODE_PIN_BINDING_VERDICT`, cascade step 0 ahead of the memoized state/static table/cache/probe) | R12-011 text-only — disclosure strings verbatim, `classifyProbeResponse` untouched (ticket 16); R13-003 harness-only pin: invalid/empty value logged + ignored, `observe` still writes, pin wins every resolve |
| load_tool + fallback | `packages/opencode/src/tool/load_tool.ts` + `load_tool.txt` (bulk load, serve off the frozen `(mode, wrapper)` pair, `delivered()` predicate over the filtered history, delivery marker via `ExecuteResult.metadata`), `packages/opencode/src/session/tool-listing.ts` (`ToolListing.withFallback`: failed deferred call appends the full schema to the error text, marks the running part loaded, excludes permission denials + aborts, feeds `InvalidArgumentsError` to `BindingVerdict.observe`; `schemaBlock` — the shared round-1 schema-block format), `packages/opencode/src/session/tools.ts` (deferred executes wrapped once at the shaped-pass confluence — built-in + resource + MCP uniformly, wrap independent of the listing view so wrapper-session deferred closures stay withFallback-wrapped for dispatch; kill-switch branch returns before the wrap), `packages/opencode/src/tool/registry.ts` (registration behind the `disableLazyTools` gate, `PromptBase.node` dep) | R12-004/005 (ticket 06): first load = full description + schema (advisory) / description only (binding), confirmation on delivered loads, re-serve after compaction (`time.compacted` respected), per-name error lines never fatal; R12-006 (ticket 07): direct call executes, failure of a not-yet-delivered tool recovers protocol-level via the schema block in the error output; R12-012 (ticket 17): binding+wrapper serves description + schema again (no listing entry carries the schema); missing frozen wrapper defaults false |
| deferred_tool meta-tool | `packages/opencode/src/tool/deferred_tool.ts` + `deferred_tool.txt` (closed `{name, args}` schema, description, `dispatch` executor), `packages/opencode/src/session/tools.ts` (confluence: meta-tool AITool constructed only when `wrapperActive`, closing over the completed shaped record), `test/tool/deferred-tool.test.ts` + `test/session/tools.test.ts` (dispatch seam + confluence tests) | R12-012 (ticket 19): dispatch validates `name` against the per-turn shaped deferred seeds only (permissible — unknown/denied ⇒ pinned "Unknown deferred tool" error, eager ⇒ "call it directly"; no schema, no marker), writes `metadata.deferred_tool = { tool }` on the running part at dispatch start (failToolCall preserves it), on unparseable/non-object `args` delivers the inner full schema block + `load_tool` marker (schema-in-error, the binding-session stand-in for R12-006), routes through the withFallback-wrapped inner closure (permission/plugin/fallback unwrap naturally — no wrapper-level triggers, no double-append), and merges `deferred_tool` into the success output's metadata (survives completeToolCall's overwrite) |
| Prompt capture | `packages/opencode/src/session/llm/prompt-capture.ts` (module) + `packages/opencode/src/session/llm/request.ts` (gated sink at `prepare`'s return; `PrepareInput.data` = app data dir threaded from `packages/opencode/src/session/llm.ts`, where `Global.Service` is a build-time dep) + `packages/opencode/src/effect/runtime-flags.ts` (`enablePromptCapture`) + app data dumps | PromptCapture: `{meta, payload:{system, tools, messages}}` per turn under `data/prompt-captures/<sessionID>/`, whitelist-serialized (R10-002: params/headers/auth never enter), stateless seq from dir scan, opt-in `OPENCODE_ENABLE_PROMPT_CAPTURE` (SC-3 capture row), dump failure logged never fatal; ticket 13. `meta.toolServers?: Record<string, string>` (ticket 23, R10-006): tool name → sanitized MCP server for the breakdown's MCP view, assembled at the dump call site from the frozen listing entries (`server !== undefined` only), absent on bypass/small turns (no frozen base) — additive optional, `version` stays 1, tolerant readers (SC-4). `meta.verdict?: "binding" | "advisory"` + `meta.binary?: string` (ticket 27, R13-003/005, SC-4): the lazy branch's resolved verdict from `StreamInput.toolVerdict` (absent when none ran — bypass/small/proxy/kill-switch turns, where `tools.resolve` returns no verdict under the flag) and the producing binary's `InstallationVersion`, assembled at the same dump call site; additive optional, harness v2 consumers (feature 13) read them tolerant-read |
| Kill-switches | `packages/opencode/src/effect/runtime-flags.ts` (pattern), per-axis `OPENCODE_*` flags (`OPENCODE_DISABLE_LAZY_TOOLS`, `OPENCODE_DISABLE_STATIC_SLIMMING`, `OPENCODE_DISABLE_TOOL_WRAPPER` (R12-012 wrapper axis — binding reverts to schema-eager round-1; lazy-tools flag dominates), `OPENCODE_ENABLE_PROMPT_CAPTURE` opt-in) | R00-013: flag set = upstream-identical per axis |
| Measurement | `packages/opencode/script/measure-usage.ts` + `test/usage-report.test.ts` (ticket 14); `packages/opencode/script/reference-workload.ts` + `script/reference-workload/` (`prompts.ts` fixed list, `fixture-repo/` deterministic tree, `stub-bin.ts` echo stub, `README.md` baseline procedure) + `test/reference-workload.test.ts` (ticket 15) | measure-usage: pure `report`/`diff` cores + run-dir readers (`manifest.json` schema-validated via exported `RunManifestSchema`, DB `step-finish` parts authoritative via `bun:sqlite` read-only, captures tolerant-read) + CLI (`report <runDir> [-o out.json]` / `diff <fork> <upstream> [-o out.json]`, loud offline errors); pairing captures↔step-finish by order, chars/4 estimator reconciled to `usage.input` (residual → history), cache columns raw; diff aligns by index with `promptsDigest` guard + mismatch warnings. reference-workload driver: `--bin/--run-dir/--model/--proxy/--stub` → one isolated run dir per run (`cwd/` fixture copy + `opencode.json` model pin/temp 0, `data/` = `XDG_DATA_HOME` + `OPENCODE_DB`, seeded `auth.json`, capture env always set — upstream ignores it, fork captures), `--proxy` adds both optimization kill-switches, prompts chained on one session via `--continue`, manifest written at completion with the workload digest (prompts + fixture content); stub bin writes deterministic step-finish rows + capture dumps + `stub-env.json` (observed env flags, incl. `OPENCODE_PIN_BINDING_VERDICT`; captures mirror the harness v2 meta — `binary: "stub"`, `verdict` mapped from the pin env `binding`/`advisory` → same, anything else absent, ticket 27); baseline procedure for the R10-005 closeout in `script/reference-workload/README.md` |
| (filled by later steps) | | Detailed-design and implementation zones append here |

## Planned — round 2 basic design (names only; nothing implemented yet)

Source: `Second Cerveau/1 PROJETS/tightcode/ARCHITECTURE/basic-design.md` round-2 section + `decisions/basic-design-03.md`. Move rows into the table above as they land.

| Zone | Planned locations | Notes |
|---|---|---|
| Deferred-tool wrapper | `packages/opencode/src/tool/deferred_tool.ts` + `deferred_tool.txt` (definition facts: closed schema `{name, args}`, description; `dispatch` executor whose metadata writes wait for the running part — bounded poll, `writeMetadataWhenRunning` — because the ai-sdk can start execute before the processor consumes the stream's `tool-call` part and the pending→running rebuild drops earlier writes; the write must land before `failToolCall` preserves running metadata), `packages/opencode/src/session/tool-listing.ts` (render `(verdict, wrapper)` policy — deferred entries omitted under binding+wrapper; EAGER gains `deferred_tool`), `packages/opencode/src/session/tools.ts` (resolve: `wrapperActive` + unconditional eager seed push + meta-tool confluence), `packages/opencode/src/session/llm/prompt-base.ts` (wrapper frozen at first write next to mode), `packages/opencode/src/session/llm/request.ts` (impose drops frozen deferred entries on wrapper sessions), `packages/opencode/src/effect/runtime-flags.ts` (`disableToolWrapper` → `OPENCODE_DISABLE_TOOL_WRAPPER`), `packages/opencode/test/session/wrapper-session.integration.test.ts` (ticket 21 end-to-end: eager-only payload + catalog, dispatch + unwrap metadata + verbatim history, schema-in-error + marker + confirmation, wrapper-mode `load_tool` serve, byte stability, kill-switch, advisory regression guard; binding forced via the verdict pin seam) | R12-012: binding+wrapper drops deferred listing entries at the payload; closed schema `{name, args}`; three-way load_tool serve; catalog blocks = keyed system-block family (`ToolListing.catalogBlocks` + render slot, ticket 18). **Tickets 17 + 18 + 19 + 21 landed**; pending: presentation consumers (ticket 20) |
| Config prompt override | `packages/core/src/v1/config/config.ts` (`system_prompt` optional record: built-in template name → inline text or `{ file }`; project-over-global via existing Config merge), `packages/opencode/src/session/llm/request.ts` (inline cascade at the position-0 seam: agent `prompt` > override > built-in; file refs resolved against `ctx.directory`, read per turn, missing ⇒ turn fails), `packages/opencode/src/session/system.ts` (`base(model)` exposes `{template, raw, name}` + `render` — extracted from `provider`'s body, `provider` output byte-identical) | R11-007 as amended (no kill-switch — capture-precedent class, `context-assembly-02`); built-in defaults untouched (R11-005); tickets 33-34 pending |
| Token estimator | `packages/opencode/script/estimator.ts` (new; `forModel(modelID)` → `estimate(text)`), tokenizer assets (committed vs pinned-download decided by the feature-10 walking-skeleton slice) | R10-007: GLM/DeepSeek `tokenizer.json` by model-id prefix, hash-pinned, lazy; o200k (`js-tiktoken`) fallback; sole owner of rendered-format parsing |
| Breakdown command | `packages/opencode/script/breakdown.ts` (new) | R10-006/009: taxonomy projection over capture dumps → versioned JSON v1 + human mode + `content` drill-down; never computed at capture time |
| Comparison harness v2 | `packages/opencode/script/measure-usage.ts` (cache-immune metric functions, campaign aggregation, v2 report), `packages/opencode/script/reference-workload.ts` (interleaved legs, verdict pin flag), `packages/opencode/script/reference-workload/README.md` (campaign procedure), manifest schema extension (binary identity, verdict, pin) | R13-001..006; real-model legs stay offline (CI unchanged) |
