# reference-fixture

A tiny deterministic repository used by the TightCode reference workload
(R10-004): fixed prompts run identically on upstream and on the fork over this
exact tree. Current version: 1.2.3.